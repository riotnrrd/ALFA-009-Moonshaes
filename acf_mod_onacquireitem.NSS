////////////////////////////////////////////////////////////////////////////////
//
//  System Name : ALFA Configuration File
//     Filename : acf_mod_onacquireitem.nss
//    $Revision:: 197        $ current version of the file
//        $Date:: 2006-12-23#$ date the file was created or modified
//       Author : Ronan
//
//  Local Variable Prefix = None
//
//  Description
//  This script calls the module's OnAcquireItem event, and any custom code added
//  by this server. It is usually used to set up server-specific options.
//  This file should NOT be overwriten in ACR updates so as to preserve any of
//  the server's customized code they may have added.
//
//  Revision History
////////////////////////////////////////////////////////////////////////////////

////////////////////////////////////////////////////////////////////////////////
// Includes ////////////////////////////////////////////////////////////////////
////////////////////////////////////////////////////////////////////////////////

#include "acr_mod_events_i"

////////////////////////////////////////////////////////////////////////////////
// Constants ///////////////////////////////////////////////////////////////////
////////////////////////////////////////////////////////////////////////////////

////////////////////////////////////////////////////////////////////////////////
// Structures //////////////////////////////////////////////////////////////////
////////////////////////////////////////////////////////////////////////////////

////////////////////////////////////////////////////////////////////////////////
// Global Variables ////////////////////////////////////////////////////////////
////////////////////////////////////////////////////////////////////////////////

////////////////////////////////////////////////////////////////////////////////
// Function Prototypes /////////////////////////////////////////////////////////
////////////////////////////////////////////////////////////////////////////////

////////////////////////////////////////////////////////////////////////////////
// Function Definitions ////////////////////////////////////////////////////////
////////////////////////////////////////////////////////////////////////////////

void main() {
    ACR_ModuleOnAcquireItem();
	/*
    // Custom code goes below this line.
	object oNewItem = GetModuleItemAcquired();
	object oGiver = GetModuleItemAcquiredFrom();
	object oPC = GetModuleItemAcquiredBy();
	SendMessageToPC(oPC, "Acquired "+GetName(oNewItem)+" from "+GetName(oGiver)+" validity: "+IntToString(GetIsObjectValid(oGiver))+", TYPE = "+IntToString(GetObjectType(oGiver)));
	if (GetObjectType(oGiver) == OBJECT_TYPE_STORE) {
		object oStore = GetNearestObject(OBJECT_TYPE_STORE, oPC);
		int nCurrentGold = GetGold(oPC);
		int nItemCost = GetLocalInt(oPC, "ACR_GOLD") - nCurrentGold;
		SendMessageToPC(oPC, "Likely bought from "+GetResRef(oStore)+" for "+IntToString(nItemCost)+" gp.");
		SetLocalString(oNewItem, "ACR_RETURNS_MERCHANT", GetResRef(oStore));
		SetLocalInt(oNewItem, "ACR_RETURNS_VALUE", nItemCost);
		SetLocalInt(oPC, "ACR_GOLD", nCurrentGold);
	} */
}